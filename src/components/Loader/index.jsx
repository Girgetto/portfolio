import React from "react";

const Loader = () => (
  <p
    style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      height: "100vh",
    }}
  >
    Loading
  </p>
);

export default Loader;

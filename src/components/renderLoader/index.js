import React from "react";

const renderLoader = () => (
  <p
    style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      height: "100vh",
    }}
  >
    Loading
  </p>
);

export default renderLoader;
